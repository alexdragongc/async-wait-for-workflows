name: CI B

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Artificial 15 seconds delay
        run: sleep 15

      - name: Hello world
        run: echo "Hello, world!"
