name: CI B

on: [push, pull_request]

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Artificial 45 seconds delay
        run: sleep 45

      - name: Hello world
        run: echo "Hello, world!"
